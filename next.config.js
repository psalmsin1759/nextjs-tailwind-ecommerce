/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'placehold.co',
      'storage.googleapis.com',
      'fakestoreapi.com',
      'bakersluxury.com',
    ],
  },
};

module.exports = nextConfig;
